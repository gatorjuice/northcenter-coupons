<%= form_with model: @business do |form| %>
  <%= render partial: 'business', locals: { f: form } %>

  <%= form.submit 'Update Business', class: 'btn btn-northcenter' %>
  <%= link_to "Delete Business", business_path(@business), method: :delete, data: { confirm: "You sure?" } %>
<% end %>
